{
  "name": "guesstock",
  "version": "1.0.0",
  "description": "A stock guess game",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",  // Sunucuyu başlatacak komut
    "build": "echo \"No build step required\" && exit 0"  // Eğer React frontend kullanmıyorsan build gerekmez
  },
  "dependencies": {
    "express": "^4.17.1",  // Express sunucu bağımlılığı
    "axios": "^0.21.1",    // API'den veri çekmek için Axios
    "cors": "^2.8.5"       // CORS hatalarını engellemek için CORS bağımlılığı
  },
  "devDependencies": {},
  "engines": {
    "node": "14.x"  // Render'da Node.js 14.x sürümünü kullanıyoruz
  },
  "author": "",
  "license": "ISC"
}
